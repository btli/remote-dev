// AUTO-GENERATED by scripts/codegen-schema.ts — DO NOT EDIT.
// Edit schema.def.ts and run `bun run db:codegen`.

import { sqliteTable, text, integer, index, primaryKey } from "drizzle-orm/sqlite-core";
import type { AdapterAccountType } from "next-auth/adapters";
import type {
  SupervisorRole,
  InstanceStatus,
  StorageTargetKind,
} from "./schema-types";

export const supervisorUser = sqliteTable(
  "supervisor_user",
  {
    id: text("id").primaryKey().$defaultFn(() => crypto.randomUUID()),
    email: text("email").notNull().unique(),
    role: text("role").$type<SupervisorRole>().notNull().default("viewer"),
    createdAt: integer("created_at", { mode: "timestamp_ms" }).notNull().$defaultFn(() => new Date()),
    updatedAt: integer("updated_at", { mode: "timestamp_ms" }).notNull().$defaultFn(() => new Date()).$onUpdateFn(() => new Date()),
  }
);

export const instance = sqliteTable(
  "instance",
  {
    id: text("id").primaryKey().$defaultFn(() => crypto.randomUUID()),
    slug: text("slug").notNull().unique(),
    displayName: text("display_name").notNull(),
    ownerId: text("owner_id").notNull().references(() => supervisorUser.id, { onDelete: "restrict" }),
    status: text("status").$type<InstanceStatus>().notNull().default("requested"),
    errorMessage: text("error_message"),
    namespace: text("namespace").notNull(),
    imageTag: text("image_tag"),
    baseUrl: text("base_url"),
    storageTargetId: text("storage_target_id"),
    storageConfigSnapshot: text("storage_config_snapshot"),
    cpuRequest: text("cpu_request"),
    cpuLimit: text("cpu_limit"),
    memRequest: text("mem_request"),
    memLimit: text("mem_limit"),
    storageRequest: text("storage_request"),
    lastReconciledAt: integer("last_reconciled_at", { mode: "timestamp_ms" }),
    provisionedAt: integer("provisioned_at", { mode: "timestamp_ms" }),
    suspendedAt: integer("suspended_at", { mode: "timestamp_ms" }),
    deletedAt: integer("deleted_at", { mode: "timestamp_ms" }),
    createdAt: integer("created_at", { mode: "timestamp_ms" }).notNull().$defaultFn(() => new Date()),
    updatedAt: integer("updated_at", { mode: "timestamp_ms" }).notNull().$defaultFn(() => new Date()).$onUpdateFn(() => new Date()),
  },
  (table) => [
    index("instance_owner_idx").on(table.ownerId),
    index("instance_status_idx").on(table.status),
  ]
);

export const registeredStorageTarget = sqliteTable(
  "registered_storage_target",
  {
    id: text("id").primaryKey().$defaultFn(() => crypto.randomUUID()),
    name: text("name").notNull().unique(),
    kind: text("kind").$type<StorageTargetKind>().notNull(),
    config: text("config").notNull(),
    resiliencyNote: text("resiliency_note"),
    isDefault: integer("is_default", { mode: "boolean" }).notNull().default(false),
    createdAt: integer("created_at", { mode: "timestamp_ms" }).notNull().$defaultFn(() => new Date()),
  }
);

export const instanceAuditLog = sqliteTable(
  "instance_audit_log",
  {
    id: text("id").primaryKey().$defaultFn(() => crypto.randomUUID()),
    instanceId: text("instance_id").notNull().references(() => instance.id, { onDelete: "cascade" }),
    actorId: text("actor_id"),
    actorEmail: text("actor_email"),
    action: text("action").notNull(),
    previousStatus: text("previous_status").$type<InstanceStatus>(),
    newStatus: text("new_status").$type<InstanceStatus>(),
    metadata: text("metadata"),
    createdAt: integer("created_at", { mode: "timestamp_ms" }).notNull().$defaultFn(() => new Date()),
  },
  (table) => [
    index("instance_audit_log_instance_idx").on(table.instanceId),
  ]
);

export const instanceSeed = sqliteTable(
  "instance_seed",
  {
    id: text("id").primaryKey().$defaultFn(() => crypto.randomUUID()),
    instanceId: text("instance_id").notNull().unique().references(() => instance.id, { onDelete: "cascade" }),
    authorizedEmails: text("authorized_emails"),
    jobDispatched: integer("job_dispatched", { mode: "boolean" }).notNull().default(false),
    jobName: text("job_name"),
    completedAt: integer("completed_at", { mode: "timestamp_ms" }),
  }
);

export const users = sqliteTable(
  "user",
  {
    id: text("id").primaryKey().$defaultFn(() => crypto.randomUUID()),
    name: text("name"),
    email: text("email").unique(),
    emailVerified: integer("emailVerified", { mode: "timestamp_ms" }),
    image: text("image"),
  }
);

export const accounts = sqliteTable(
  "account",
  {
    userId: text("userId").notNull().references(() => users.id, { onDelete: "cascade" }),
    type: text("type").$type<AdapterAccountType>().notNull(),
    provider: text("provider").notNull(),
    providerAccountId: text("providerAccountId").notNull(),
    refresh_token: text("refresh_token"),
    access_token: text("access_token"),
    expires_at: integer("expires_at"),
    token_type: text("token_type"),
    scope: text("scope"),
    id_token: text("id_token"),
    session_state: text("session_state"),
  },
  (table) => [
    primaryKey({ columns: [table.provider, table.providerAccountId] }),
    index("account_user_idx").on(table.userId),
  ]
);

export const sessions = sqliteTable(
  "session",
  {
    sessionToken: text("sessionToken").primaryKey(),
    userId: text("userId").notNull().references(() => users.id, { onDelete: "cascade" }),
    expires: integer("expires", { mode: "timestamp_ms" }).notNull(),
  }
);

export const verificationTokens = sqliteTable(
  "verificationToken",
  {
    identifier: text("identifier").notNull(),
    token: text("token").notNull(),
    expires: integer("expires", { mode: "timestamp_ms" }).notNull(),
  },
  (table) => [
    primaryKey({ columns: [table.identifier, table.token] }),
  ]
);
