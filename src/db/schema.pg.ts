// AUTO-GENERATED by scripts/codegen-schema.ts — DO NOT EDIT.
// Edit schema.def.ts and run `bun run db:codegen`.

import { pgTable, text, integer, boolean, timestamp, jsonb, index, uniqueIndex, primaryKey } from "drizzle-orm/pg-core";
import type { AnyPgColumn } from "drizzle-orm/pg-core";
import type { AdapterAccountType } from "next-auth/adapters";
import type { SessionStatus } from "@/types/session";
import type { CIStatusState, PRState } from "@/types/github-stats";
import type { ScheduleType, ScheduleStatus, ExecutionStatus } from "@/types/schedule";
import type { AgentProvider, AgentConfigType, MCPTransport } from "@/types/agent";
import type { AgentProviderType, WorktreeType } from "@/types/session";
import type { TerminalType, AgentExitState } from "@/types/terminal-type";
import type { AppearanceMode, ColorSchemeCategory, ColorSchemeId } from "@/types/appearance";
import type { TaskPriority, TaskStatus, TaskSource } from "@/types/task";
import type { NotificationType, NotificationSeverity, NotificationMeta } from "@/types/notification";
import type { ChannelType } from "@/types/channels";
import type { AgentRunStatus, AgentRunSource, TriggerKind } from "@/types/agent-run";
import type { CrownRunStatus } from "@/types/crown";

export const users = pgTable(
  "user",
  {
    id: text("id").primaryKey().$defaultFn(() => crypto.randomUUID()),
    name: text("name"),
    email: text("email").unique(),
    emailVerified: timestamp("emailVerified", { withTimezone: true, mode: "date" }),
    image: text("image"),
  }
);

export const userEmails = pgTable(
  "user_email",
  {
    id: text("id").primaryKey().$defaultFn(() => crypto.randomUUID()),
    userId: text("user_id").notNull().references(() => users.id, { onDelete: "cascade" }),
    email: text("email").notNull().unique(),
    isPrimary: boolean("is_primary").notNull().default(false),
    createdAt: timestamp("created_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
  },
  (table) => [
    index("user_email_user_idx").on(table.userId),
  ]
);

export const accounts = pgTable(
  "account",
  {
    userId: text("userId").notNull().references(() => users.id, { onDelete: "cascade" }),
    type: text("type").$type<AdapterAccountType>().notNull(),
    provider: text("provider").notNull(),
    providerAccountId: text("providerAccountId").notNull(),
    refresh_token: text("refresh_token"),
    access_token: text("access_token"),
    expires_at: integer("expires_at"),
    token_type: text("token_type"),
    scope: text("scope"),
    id_token: text("id_token"),
    session_state: text("session_state"),
  },
  (table) => [
    primaryKey({ columns: [table.provider, table.providerAccountId] }),
    index("account_user_idx").on(table.userId),
  ]
);

export const sessions = pgTable(
  "session",
  {
    sessionToken: text("sessionToken").primaryKey(),
    userId: text("userId").notNull().references(() => users.id, { onDelete: "cascade" }),
    expires: timestamp("expires", { withTimezone: true, mode: "date" }).notNull(),
  }
);

export const verificationTokens = pgTable(
  "verificationToken",
  {
    identifier: text("identifier").notNull(),
    token: text("token").notNull(),
    expires: timestamp("expires", { withTimezone: true, mode: "date" }).notNull(),
  },
  (table) => [
    primaryKey({ columns: [table.identifier, table.token] }),
  ]
);

export const authorizedUsers = pgTable(
  "authorized_user",
  {
    id: text("id").primaryKey().$defaultFn(() => crypto.randomUUID()),
    email: text("email").notNull().unique(),
    createdAt: timestamp("created_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
  }
);

export const userSettings = pgTable(
  "user_settings",
  {
    id: text("id").primaryKey().$defaultFn(() => crypto.randomUUID()),
    userId: text("user_id").notNull().unique().references(() => users.id, { onDelete: "cascade" }),
    defaultWorkingDirectory: text("default_working_directory"),
    defaultShell: text("default_shell"),
    startupCommand: text("startup_command"),
    xtermScrollback: integer("xterm_scrollback").default(10000),
    tmuxHistoryLimit: integer("tmux_history_limit").default(50000),
    theme: text("theme").default("tokyo-night"),
    fontSize: integer("font_size").default(14),
    fontFamily: text("font_family").default("'JetBrainsMono Nerd Font Mono', monospace"),
    activeNodeId: text("active_node_id"),
    activeNodeType: text("active_node_type", { enum: ["group","project"] }),
    pinnedNodeId: text("pinned_node_id"),
    pinnedNodeType: text("pinned_node_type", { enum: ["group","project"] }),
    autoFollowActiveSession: boolean("auto_follow_active_session").notNull().default(true),
    notificationsEnabled: boolean("notifications_enabled").notNull().default(true),
    defaultAgentProvider: text("default_agent_provider"),
    agentProviderSettings: jsonb("agent_provider_settings"),
    beadsSidebarCollapsed: boolean("beads_sidebar_collapsed").notNull().default(true),
    beadsSidebarWidth: integer("beads_sidebar_width").default(320),
    beadsClosedRetentionDays: integer("beads_closed_retention_days").default(7),
    beadsSectionExpanded: text("beads_section_expanded"),
    createdAt: timestamp("created_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
    updatedAt: timestamp("updated_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
  }
);

export const githubRepositories = pgTable(
  "github_repository",
  {
    id: text("id").primaryKey().$defaultFn(() => crypto.randomUUID()),
    userId: text("user_id").notNull().references(() => users.id, { onDelete: "cascade" }),
    githubId: integer("github_id").notNull(),
    name: text("name").notNull(),
    fullName: text("full_name").notNull(),
    cloneUrl: text("clone_url").notNull(),
    defaultBranch: text("default_branch").notNull(),
    localPath: text("local_path"),
    isPrivate: boolean("is_private").notNull().default(false),
    addedAt: timestamp("added_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
    updatedAt: timestamp("updated_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
  },
  (table) => [
    index("github_repo_user_idx").on(table.userId),
    index("github_repo_github_id_idx").on(table.userId, table.githubId),
  ]
);

export const githubAccountMetadata = pgTable(
  "github_account_metadata",
  {
    providerAccountId: text("provider_account_id").primaryKey(),
    userId: text("user_id").notNull().references(() => users.id, { onDelete: "cascade" }),
    login: text("login").notNull(),
    displayName: text("display_name"),
    avatarUrl: text("avatar_url").notNull(),
    email: text("email"),
    isDefault: boolean("is_default").notNull().default(false),
    configDir: text("config_dir").notNull(),
    createdAt: timestamp("created_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
    updatedAt: timestamp("updated_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
  },
  (table) => [
    index("github_account_metadata_user_idx").on(table.userId),
  ]
);

export const portRegistry = pgTable(
  "port_registry",
  {
    id: text("id").primaryKey().$defaultFn(() => crypto.randomUUID()),
    projectId: text("project_id").references(() => projects.id, { onDelete: "cascade" }),
    userId: text("user_id").notNull().references(() => users.id, { onDelete: "cascade" }),
    port: integer("port").notNull(),
    variableName: text("variable_name").notNull(),
    createdAt: timestamp("created_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
  },
  (table) => [
    index("port_registry_user_idx").on(table.userId),
    index("port_registry_project_idx").on(table.projectId),
    index("port_registry_user_port_idx").on(table.userId, table.port),
    uniqueIndex("port_registry_user_port_var_unique").on(table.userId, table.port, table.variableName),
  ]
);

export const portClaims = pgTable(
  "port_claim",
  {
    id: text("id").primaryKey().$defaultFn(() => crypto.randomUUID()),
    sessionId: text("session_id").notNull().references(() => terminalSessions.id, { onDelete: "cascade" }),
    userId: text("user_id").notNull().references(() => users.id, { onDelete: "cascade" }),
    projectId: text("project_id").references(() => projects.id, { onDelete: "cascade" }),
    port: integer("port").notNull(),
    variableName: text("variable_name").notNull(),
    isListening: boolean("is_listening"),
    pid: integer("pid"),
    expiresAt: timestamp("expires_at", { withTimezone: true, mode: "date" }).notNull(),
    claimedAt: timestamp("claimed_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
    updatedAt: timestamp("updated_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
  },
  (table) => [
    index("port_claim_session_idx").on(table.sessionId),
    index("port_claim_user_idx").on(table.userId),
    index("port_claim_port_idx").on(table.port),
    uniqueIndex("port_claim_session_port_unique").on(table.sessionId, table.port),
  ]
);

export const sessionTemplates = pgTable(
  "session_template",
  {
    id: text("id").primaryKey().$defaultFn(() => crypto.randomUUID()),
    userId: text("user_id").notNull().references(() => users.id, { onDelete: "cascade" }),
    name: text("name").notNull(),
    description: text("description"),
    sessionNamePattern: text("session_name_pattern"),
    projectPath: text("project_path"),
    startupCommand: text("startup_command"),
    projectId: text("project_id").references(() => projects.id, { onDelete: "set null" }),
    icon: text("icon"),
    theme: text("theme"),
    fontSize: integer("font_size"),
    fontFamily: text("font_family"),
    usageCount: integer("usage_count").notNull().default(0),
    lastUsedAt: timestamp("last_used_at", { withTimezone: true, mode: "date" }),
    createdAt: timestamp("created_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
    updatedAt: timestamp("updated_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
  },
  (table) => [
    index("session_template_user_idx").on(table.userId),
    index("session_template_usage_idx").on(table.userId, table.usageCount),
    index("session_template_project_idx").on(table.projectId),
  ]
);

export const sessionRecordings = pgTable(
  "session_recording",
  {
    id: text("id").primaryKey().$defaultFn(() => crypto.randomUUID()),
    userId: text("user_id").notNull().references(() => users.id, { onDelete: "cascade" }),
    sessionId: text("session_id"),
    name: text("name").notNull(),
    description: text("description"),
    duration: integer("duration").notNull(),
    terminalCols: integer("terminal_cols").notNull().default(80),
    terminalRows: integer("terminal_rows").notNull().default(24),
    data: text("data").notNull(),
    createdAt: timestamp("created_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
  },
  (table) => [
    index("session_recording_user_idx").on(table.userId),
    index("session_recording_created_idx").on(table.userId, table.createdAt),
  ]
);

export const apiKeys = pgTable(
  "api_key",
  {
    id: text("id").primaryKey().$defaultFn(() => crypto.randomUUID()),
    userId: text("user_id").notNull().references(() => users.id, { onDelete: "cascade" }),
    name: text("name").notNull(),
    keyPrefix: text("key_prefix").notNull(),
    keyHash: text("key_hash").notNull(),
    lastUsedAt: timestamp("last_used_at", { withTimezone: true, mode: "date" }),
    expiresAt: timestamp("expires_at", { withTimezone: true, mode: "date" }),
    createdAt: timestamp("created_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
  },
  (table) => [
    index("api_key_user_idx").on(table.userId),
    index("api_key_prefix_idx").on(table.keyPrefix),
  ]
);

export const modelProxyTokens = pgTable(
  "model_proxy_token",
  {
    id: text("id").primaryKey().$defaultFn(() => crypto.randomUUID()),
    userId: text("user_id").notNull().references(() => users.id, { onDelete: "cascade" }),
    sessionId: text("session_id").references(() => terminalSessions.id, { onDelete: "cascade" }),
    instanceSlug: text("instance_slug"),
    tokenPrefix: text("token_prefix").notNull(),
    tokenHash: text("token_hash").notNull(),
    providerScope: text("provider_scope").notNull().default("[\"anthropic\"]"),
    revokedAt: timestamp("revoked_at", { withTimezone: true, mode: "date" }),
    expiresAt: timestamp("expires_at", { withTimezone: true, mode: "date" }),
    lastUsedAt: timestamp("last_used_at", { withTimezone: true, mode: "date" }),
    createdAt: timestamp("created_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
  },
  (table) => [
    index("model_proxy_token_prefix_idx").on(table.tokenPrefix),
    index("model_proxy_token_session_idx").on(table.sessionId),
    index("model_proxy_token_user_idx").on(table.userId),
  ]
);

export const modelUsageEvents = pgTable(
  "model_usage_event",
  {
    id: text("id").primaryKey().$defaultFn(() => crypto.randomUUID()),
    userId: text("user_id").notNull(),
    sessionId: text("session_id"),
    instanceSlug: text("instance_slug"),
    provider: text("provider").notNull(),
    model: text("model"),
    inputTokens: integer("input_tokens").notNull().default(0),
    outputTokens: integer("output_tokens").notNull().default(0),
    cacheReadTokens: integer("cache_read_tokens").notNull().default(0),
    cacheCreationTokens: integer("cache_creation_tokens").notNull().default(0),
    costMicroUsd: integer("cost_micro_usd"),
    createdAt: timestamp("created_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
  },
  (table) => [
    index("model_usage_session_idx").on(table.sessionId),
    index("model_usage_user_idx").on(table.userId),
    index("model_usage_instance_idx").on(table.instanceSlug),
    index("model_usage_created_idx").on(table.createdAt),
  ]
);

export const terminalSessions = pgTable(
  "terminal_session",
  {
    id: text("id").primaryKey().$defaultFn(() => crypto.randomUUID()),
    userId: text("user_id").notNull().references(() => users.id, { onDelete: "cascade" }),
    name: text("name").notNull(),
    tmuxSessionName: text("tmux_session_name").notNull().unique(),
    projectPath: text("project_path"),
    githubRepoId: text("github_repo_id").references(() => githubRepositories.id, { onDelete: "set null" }),
    worktreeBranch: text("worktree_branch"),
    worktreeType: text("worktree_type").$type<WorktreeType>(),
    projectId: text("project_id").notNull().references(() => projects.id, { onDelete: "cascade" }),
    profileId: text("profile_id").references(() => agentProfiles.id, { onDelete: "set null" }),
    terminalType: text("terminal_type").$type<TerminalType>().default("shell"),
    agentProvider: text("agent_provider").$type<AgentProviderType>(),
    agentExitState: text("agent_exit_state").$type<AgentExitState>(),
    agentExitCode: integer("agent_exit_code"),
    agentExitedAt: timestamp("agent_exited_at", { withTimezone: true, mode: "date" }),
    agentRestartCount: integer("agent_restart_count").default(0),
    agentActivityStatus: text("agent_activity_status"),
    typeMetadata: text("type_metadata"),
    scopeKey: text("scope_key"),
    parentSessionId: text("parent_session_id"),
    orchestratorRole: text("orchestrator_role").$type<"parent" | "child">(),
    status: text("status").$type<SessionStatus>().notNull().default("active"),
    pinned: boolean("pinned").notNull().default(false),
    tabOrder: integer("tab_order").notNull().default(0),
    lastActivityAt: timestamp("last_activity_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
    createdAt: timestamp("created_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
    updatedAt: timestamp("updated_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
  },
  (table) => [
    index("terminal_session_user_status_idx").on(table.userId, table.status),
    index("terminal_session_user_order_idx").on(table.userId, table.tabOrder),
    index("terminal_session_project_idx").on(table.projectId),
    index("terminal_session_type_idx").on(table.userId, table.terminalType),
    uniqueIndex("terminal_session_scope_unique_idx").on(table.userId, table.terminalType, table.scopeKey),
  ]
);

export const trashItems = pgTable(
  "trash_item",
  {
    id: text("id").primaryKey().$defaultFn(() => crypto.randomUUID()),
    userId: text("user_id").notNull().references(() => users.id, { onDelete: "cascade" }),
    resourceType: text("resource_type").notNull(),
    resourceId: text("resource_id").notNull(),
    resourceName: text("resource_name").notNull(),
    trashedAt: timestamp("trashed_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
    expiresAt: timestamp("expires_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date(Date.now() + 30 * 24 * 60 * 60 * 1000)),
  },
  (table) => [
    index("trash_item_user_type_idx").on(table.userId, table.resourceType),
    index("trash_item_expires_idx").on(table.expiresAt),
    index("trash_item_resource_idx").on(table.resourceType, table.resourceId),
  ]
);

export const worktreeTrashMetadata = pgTable(
  "worktree_trash_metadata",
  {
    id: text("id").primaryKey().$defaultFn(() => crypto.randomUUID()),
    trashItemId: text("trash_item_id").notNull().unique().references(() => trashItems.id, { onDelete: "cascade" }),
    githubRepoId: text("github_repo_id").references(() => githubRepositories.id, { onDelete: "set null" }),
    repoName: text("repo_name").notNull(),
    repoLocalPath: text("repo_local_path").notNull(),
    worktreeBranch: text("worktree_branch").notNull(),
    worktreeOriginalPath: text("worktree_original_path").notNull(),
    worktreeTrashPath: text("worktree_trash_path").notNull(),
    originalProjectId: text("original_project_id"),
    originalProjectName: text("original_project_name"),
    createdAt: timestamp("created_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
  },
  (table) => [
    index("worktree_trash_repo_idx").on(table.githubRepoId),
  ]
);

export const githubRepositoryStats = pgTable(
  "github_repository_stats",
  {
    id: text("id").primaryKey().$defaultFn(() => crypto.randomUUID()),
    repositoryId: text("repository_id").notNull().unique().references(() => githubRepositories.id, { onDelete: "cascade" }),
    openPRCount: integer("open_pr_count").notNull().default(0),
    openIssueCount: integer("open_issue_count").notNull().default(0),
    ciStatus: text("ci_status").$type<CIStatusState>(),
    ciStatusDetails: text("ci_status_details"),
    branchProtected: boolean("branch_protected").notNull().default(false),
    branchProtectionDetails: text("branch_protection_details"),
    recentCommits: text("recent_commits"),
    cachedAt: timestamp("cached_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
    expiresAt: timestamp("expires_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date(Date.now() + 15 * 60 * 1000)),
  },
  (table) => [
    index("github_repo_stats_repo_idx").on(table.repositoryId),
    index("github_repo_stats_expires_idx").on(table.expiresAt),
  ]
);

export const githubPullRequests = pgTable(
  "github_pull_request",
  {
    id: text("id").primaryKey().$defaultFn(() => crypto.randomUUID()),
    repositoryId: text("repository_id").notNull().references(() => githubRepositories.id, { onDelete: "cascade" }),
    prNumber: integer("pr_number").notNull(),
    title: text("title").notNull(),
    state: text("state").$type<PRState>().notNull(),
    branch: text("branch").notNull(),
    baseBranch: text("base_branch").notNull(),
    author: text("author").notNull(),
    authorAvatarUrl: text("author_avatar_url"),
    url: text("url").notNull(),
    isDraft: boolean("is_draft").notNull().default(false),
    additions: integer("additions").notNull().default(0),
    deletions: integer("deletions").notNull().default(0),
    reviewDecision: text("review_decision").$type<"APPROVED" | "CHANGES_REQUESTED" | "REVIEW_REQUIRED">(),
    ciStatus: text("ci_status").$type<CIStatusState>(),
    isNew: boolean("is_new").notNull().default(false),
    createdAt: timestamp("created_at", { withTimezone: true, mode: "date" }).notNull(),
    updatedAt: timestamp("updated_at", { withTimezone: true, mode: "date" }).notNull(),
    cachedAt: timestamp("cached_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
  },
  (table) => [
    index("github_pr_repo_idx").on(table.repositoryId),
    index("github_pr_repo_number_idx").on(table.repositoryId, table.prNumber),
    index("github_pr_state_idx").on(table.repositoryId, table.state),
  ]
);

export const githubBranchProtection = pgTable(
  "github_branch_protection",
  {
    id: text("id").primaryKey().$defaultFn(() => crypto.randomUUID()),
    repositoryId: text("repository_id").notNull().references(() => githubRepositories.id, { onDelete: "cascade" }),
    branch: text("branch").notNull(),
    isProtected: boolean("is_protected").notNull().default(false),
    requiresReview: boolean("requires_review").notNull().default(false),
    requiredReviewers: integer("required_reviewers").notNull().default(0),
    requiresStatusChecks: boolean("requires_status_checks").notNull().default(false),
    requiredChecks: text("required_checks"),
    allowsForcePushes: boolean("allows_force_pushes").notNull().default(false),
    allowsDeletions: boolean("allows_deletions").notNull().default(false),
    cachedAt: timestamp("cached_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
  },
  (table) => [
    uniqueIndex("github_branch_protection_repo_branch_idx").on(table.repositoryId, table.branch),
  ]
);

export const githubStatsPreferences = pgTable(
  "github_stats_preferences",
  {
    id: text("id").primaryKey().$defaultFn(() => crypto.randomUUID()),
    userId: text("user_id").notNull().references(() => users.id, { onDelete: "cascade" }),
    projectId: text("project_id").references(() => projects.id, { onDelete: "cascade" }),
    showPRCount: boolean("show_pr_count").notNull().default(true),
    showIssueCount: boolean("show_issue_count").notNull().default(true),
    showCIStatus: boolean("show_ci_status").notNull().default(true),
    showRecentCommits: boolean("show_recent_commits").notNull().default(true),
    showBranchProtection: boolean("show_branch_protection").notNull().default(true),
    refreshIntervalMinutes: integer("refresh_interval_minutes").notNull().default(15),
    createdAt: timestamp("created_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
    updatedAt: timestamp("updated_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
  },
  (table) => [
    index("github_stats_prefs_user_idx").on(table.userId),
    index("github_stats_prefs_project_idx").on(table.projectId),
  ]
);

export const githubChangeNotifications = pgTable(
  "github_change_notification",
  {
    id: text("id").primaryKey().$defaultFn(() => crypto.randomUUID()),
    userId: text("user_id").notNull().references(() => users.id, { onDelete: "cascade" }),
    repositoryId: text("repository_id").notNull().references(() => githubRepositories.id, { onDelete: "cascade" }),
    newPRCount: integer("new_pr_count").notNull().default(0),
    newIssueCount: integer("new_issue_count").notNull().default(0),
    lastSeenAt: timestamp("last_seen_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
    createdAt: timestamp("created_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
  },
  (table) => [
    uniqueIndex("github_notifications_user_repo_idx").on(table.userId, table.repositoryId),
    index("github_notifications_user_idx").on(table.userId),
  ]
);

export const githubIssues = pgTable(
  "github_issue",
  {
    id: text("id").primaryKey().$defaultFn(() => crypto.randomUUID()),
    repositoryId: text("repository_id").notNull().references(() => githubRepositories.id, { onDelete: "cascade" }),
    issueNumber: integer("issue_number").notNull(),
    title: text("title").notNull(),
    state: text("state").$type<"open" | "closed">().notNull(),
    body: text("body"),
    htmlUrl: text("html_url").notNull(),
    author: text("author"),
    labels: text("labels").notNull().default("[]"),
    assignees: text("assignees").notNull().default("[]"),
    milestone: text("milestone"),
    comments: integer("comments").notNull().default(0),
    isPullRequest: boolean("is_pull_request").notNull().default(false),
    isNew: boolean("is_new").notNull().default(false),
    createdAt: timestamp("created_at", { withTimezone: true, mode: "date" }).notNull(),
    updatedAt: timestamp("updated_at", { withTimezone: true, mode: "date" }).notNull(),
    cachedAt: timestamp("cached_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
  },
  (table) => [
    index("github_issue_repo_idx").on(table.repositoryId),
    uniqueIndex("github_issue_repo_number_idx").on(table.repositoryId, table.issueNumber),
    index("github_issue_state_idx").on(table.repositoryId, table.state),
    index("github_issue_cached_idx").on(table.cachedAt),
  ]
);

export const sessionSchedules = pgTable(
  "session_schedule",
  {
    id: text("id").primaryKey().$defaultFn(() => crypto.randomUUID()),
    userId: text("user_id").notNull().references(() => users.id, { onDelete: "cascade" }),
    sessionId: text("session_id").notNull().references(() => terminalSessions.id, { onDelete: "cascade" }),
    name: text("name").notNull(),
    scheduleType: text("schedule_type").$type<ScheduleType>().notNull().default("one-time"),
    cronExpression: text("cron_expression"),
    scheduledAt: timestamp("scheduled_at", { withTimezone: true, mode: "date" }),
    timezone: text("timezone").notNull().default("America/Los_Angeles"),
    enabled: boolean("enabled").notNull().default(true),
    status: text("status").$type<ScheduleStatus>().notNull().default("active"),
    maxRetries: integer("max_retries").notNull().default(0),
    retryDelaySeconds: integer("retry_delay_seconds").notNull().default(60),
    timeoutSeconds: integer("timeout_seconds").notNull().default(300),
    lastRunAt: timestamp("last_run_at", { withTimezone: true, mode: "date" }),
    lastRunStatus: text("last_run_status").$type<ExecutionStatus>(),
    nextRunAt: timestamp("next_run_at", { withTimezone: true, mode: "date" }),
    consecutiveFailures: integer("consecutive_failures").notNull().default(0),
    createdAt: timestamp("created_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
    updatedAt: timestamp("updated_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
  },
  (table) => [
    index("session_schedule_user_idx").on(table.userId),
    index("session_schedule_session_idx").on(table.sessionId),
    index("session_schedule_next_run_idx").on(table.enabled, table.nextRunAt),
  ]
);

export const scheduleCommands = pgTable(
  "schedule_command",
  {
    id: text("id").primaryKey().$defaultFn(() => crypto.randomUUID()),
    scheduleId: text("schedule_id").notNull().references(() => sessionSchedules.id, { onDelete: "cascade" }),
    command: text("command").notNull(),
    order: integer("order").notNull(),
    delayBeforeSeconds: integer("delay_before_seconds").notNull().default(0),
    continueOnError: boolean("continue_on_error").notNull().default(false),
    createdAt: timestamp("created_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
  },
  (table) => [
    index("schedule_command_schedule_idx").on(table.scheduleId),
    index("schedule_command_order_idx").on(table.scheduleId, table.order),
  ]
);

export const scheduleExecutions = pgTable(
  "schedule_execution",
  {
    id: text("id").primaryKey().$defaultFn(() => crypto.randomUUID()),
    scheduleId: text("schedule_id").notNull().references(() => sessionSchedules.id, { onDelete: "cascade" }),
    status: text("status").$type<ExecutionStatus>().notNull(),
    startedAt: timestamp("started_at", { withTimezone: true, mode: "date" }).notNull(),
    completedAt: timestamp("completed_at", { withTimezone: true, mode: "date" }).notNull(),
    durationMs: integer("duration_ms").notNull(),
    commandCount: integer("command_count").notNull(),
    successCount: integer("success_count").notNull(),
    failureCount: integer("failure_count").notNull(),
    errorMessage: text("error_message"),
    output: text("output"),
  },
  (table) => [
    index("schedule_execution_schedule_idx").on(table.scheduleId),
    index("schedule_execution_started_idx").on(table.scheduleId, table.startedAt),
  ]
);

export const commandExecutions = pgTable(
  "command_execution",
  {
    id: text("id").primaryKey().$defaultFn(() => crypto.randomUUID()),
    executionId: text("execution_id").notNull().references(() => scheduleExecutions.id, { onDelete: "cascade" }),
    commandId: text("command_id").notNull().references(() => scheduleCommands.id, { onDelete: "cascade" }),
    command: text("command").notNull(),
    status: text("status").$type<ExecutionStatus>().notNull(),
    exitCode: integer("exit_code"),
    startedAt: timestamp("started_at", { withTimezone: true, mode: "date" }).notNull(),
    completedAt: timestamp("completed_at", { withTimezone: true, mode: "date" }).notNull(),
    durationMs: integer("duration_ms").notNull(),
    output: text("output"),
    errorMessage: text("error_message"),
  },
  (table) => [
    index("command_execution_execution_idx").on(table.executionId),
    index("command_execution_command_idx").on(table.commandId),
  ]
);

export const setupConfig = pgTable(
  "setup_config",
  {
    id: text("id").primaryKey().$defaultFn(() => crypto.randomUUID()),
    workingDirectory: text("working_directory").notNull(),
    nextPort: integer("next_port").notNull().default(3000),
    terminalPort: integer("terminal_port").notNull().default(3001),
    wslDistribution: text("wsl_distribution"),
    autoStart: boolean("auto_start").notNull().default(true),
    checkForUpdates: boolean("check_for_updates").notNull().default(true),
    isComplete: boolean("is_complete").notNull().default(false),
    completedAt: timestamp("completed_at", { withTimezone: true, mode: "date" }),
    createdAt: timestamp("created_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
    updatedAt: timestamp("updated_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
  }
);

export const agentProfiles = pgTable(
  "agent_profile",
  {
    id: text("id").primaryKey().$defaultFn(() => crypto.randomUUID()),
    userId: text("user_id").notNull().references(() => users.id, { onDelete: "cascade" }),
    name: text("name").notNull(),
    description: text("description"),
    provider: text("provider").$type<AgentProvider>().notNull().default("all"),
    configDir: text("config_dir").notNull(),
    isDefault: boolean("is_default").notNull().default(false),
    createdAt: timestamp("created_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
    updatedAt: timestamp("updated_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
  },
  (table) => [
    index("agent_profile_user_idx").on(table.userId),
    index("agent_profile_default_idx").on(table.userId, table.isDefault),
  ]
);

export const agentConfigs = pgTable(
  "agent_config",
  {
    id: text("id").primaryKey().$defaultFn(() => crypto.randomUUID()),
    userId: text("user_id").notNull().references(() => users.id, { onDelete: "cascade" }),
    projectId: text("project_id").references(() => projects.id, { onDelete: "cascade" }),
    provider: text("provider").$type<AgentProvider>().notNull(),
    configType: text("config_type").$type<AgentConfigType>().notNull(),
    content: text("content").notNull().default(""),
    createdAt: timestamp("created_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
    updatedAt: timestamp("updated_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
  },
  (table) => [
    index("agent_config_user_idx").on(table.userId),
    index("agent_config_project_idx").on(table.projectId),
    uniqueIndex("agent_config_unique_idx").on(table.userId, table.projectId, table.provider, table.configType),
  ]
);

export const mcpServers = pgTable(
  "mcp_server",
  {
    id: text("id").primaryKey().$defaultFn(() => crypto.randomUUID()),
    userId: text("user_id").notNull().references(() => users.id, { onDelete: "cascade" }),
    projectId: text("project_id").references(() => projects.id, { onDelete: "cascade" }),
    name: text("name").notNull(),
    transport: text("transport").$type<MCPTransport>().notNull().default("stdio"),
    command: text("command").notNull(),
    args: text("args").notNull().default("[]"),
    env: text("env").notNull().default("{}"),
    enabled: boolean("enabled").notNull().default(true),
    autoStart: boolean("auto_start").notNull().default(false),
    lastHealthCheck: timestamp("last_health_check", { withTimezone: true, mode: "date" }),
    createdAt: timestamp("created_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
    updatedAt: timestamp("updated_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
  },
  (table) => [
    index("mcp_server_user_idx").on(table.userId),
    index("mcp_server_project_idx").on(table.projectId),
    index("mcp_server_enabled_idx").on(table.userId, table.enabled),
  ]
);

export const profileGitIdentities = pgTable(
  "profile_git_identity",
  {
    id: text("id").primaryKey().$defaultFn(() => crypto.randomUUID()),
    profileId: text("profile_id").notNull().unique().references(() => agentProfiles.id, { onDelete: "cascade" }),
    userName: text("user_name").notNull(),
    userEmail: text("user_email").notNull(),
    sshKeyPath: text("ssh_key_path"),
    gpgKeyId: text("gpg_key_id"),
    githubUsername: text("github_username"),
    createdAt: timestamp("created_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
    updatedAt: timestamp("updated_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
  },
  (table) => [
    index("profile_git_identity_profile_idx").on(table.profileId),
  ]
);

export const profileSecretsConfig = pgTable(
  "profile_secrets_config",
  {
    id: text("id").primaryKey().$defaultFn(() => crypto.randomUUID()),
    profileId: text("profile_id").notNull().unique().references(() => agentProfiles.id, { onDelete: "cascade" }),
    userId: text("user_id").notNull().references(() => users.id, { onDelete: "cascade" }),
    provider: text("provider").notNull(),
    providerConfig: text("provider_config").notNull(),
    enabled: boolean("enabled").notNull().default(true),
    lastFetchedAt: timestamp("last_fetched_at", { withTimezone: true, mode: "date" }),
    createdAt: timestamp("created_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
    updatedAt: timestamp("updated_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
  },
  (table) => [
    index("profile_secrets_config_profile_idx").on(table.profileId),
    index("profile_secrets_config_user_idx").on(table.userId),
  ]
);

export const mcpDiscoveredTools = pgTable(
  "mcp_discovered_tool",
  {
    id: text("id").primaryKey().$defaultFn(() => crypto.randomUUID()),
    serverId: text("server_id").notNull().references(() => mcpServers.id, { onDelete: "cascade" }),
    name: text("name").notNull(),
    description: text("description"),
    inputSchema: text("input_schema"),
    discoveredAt: timestamp("discovered_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
  },
  (table) => [
    index("mcp_discovered_tool_server_idx").on(table.serverId),
    uniqueIndex("mcp_discovered_tool_unique_idx").on(table.serverId, table.name),
  ]
);

export const mcpDiscoveredResources = pgTable(
  "mcp_discovered_resource",
  {
    id: text("id").primaryKey().$defaultFn(() => crypto.randomUUID()),
    serverId: text("server_id").notNull().references(() => mcpServers.id, { onDelete: "cascade" }),
    uri: text("uri").notNull(),
    name: text("name"),
    description: text("description"),
    mimeType: text("mime_type"),
    discoveredAt: timestamp("discovered_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
  },
  (table) => [
    index("mcp_discovered_resource_server_idx").on(table.serverId),
    uniqueIndex("mcp_discovered_resource_unique_idx").on(table.serverId, table.uri),
  ]
);

export const agentActivityEvents = pgTable(
  "agent_activity_event",
  {
    id: text("id").primaryKey().$defaultFn(() => crypto.randomUUID()),
    userId: text("user_id").notNull().references(() => users.id, { onDelete: "cascade" }),
    sessionId: text("session_id").references(() => terminalSessions.id, { onDelete: "set null" }),
    agentProvider: text("agent_provider").$type<AgentProviderType>(),
    eventType: text("event_type").notNull(),
    eventData: text("event_data"),
    duration: integer("duration"),
    success: boolean("success"),
    errorMessage: text("error_message"),
    createdAt: timestamp("created_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
  },
  (table) => [
    index("agent_activity_user_idx").on(table.userId),
    index("agent_activity_session_idx").on(table.sessionId),
    index("agent_activity_provider_idx").on(table.userId, table.agentProvider),
    index("agent_activity_event_type_idx").on(table.userId, table.eventType),
    index("agent_activity_created_idx").on(table.userId, table.createdAt),
  ]
);

export const agentDailyStats = pgTable(
  "agent_daily_stats",
  {
    id: text("id").primaryKey().$defaultFn(() => crypto.randomUUID()),
    userId: text("user_id").notNull().references(() => users.id, { onDelete: "cascade" }),
    date: text("date").notNull(),
    agentProvider: text("agent_provider").$type<AgentProviderType>(),
    sessionCount: integer("session_count").notNull().default(0),
    commandCount: integer("command_count").notNull().default(0),
    errorCount: integer("error_count").notNull().default(0),
    totalDuration: integer("total_duration").notNull().default(0),
    toolCallCount: integer("tool_call_count").notNull().default(0),
    updatedAt: timestamp("updated_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
  },
  (table) => [
    uniqueIndex("agent_daily_stats_unique_idx").on(table.userId, table.date, table.agentProvider),
    index("agent_daily_stats_user_date_idx").on(table.userId, table.date),
  ]
);

export const sessionMemory = pgTable(
  "session_memory",
  {
    id: text("id").primaryKey().$defaultFn(() => crypto.randomUUID()),
    userId: text("user_id").notNull().references(() => users.id, { onDelete: "cascade" }),
    projectId: text("project_id").references(() => projects.id, { onDelete: "set null" }),
    type: text("type").$type<"note" | "artifact" | "summary">().notNull(),
    title: text("title").notNull(),
    content: text("content").notNull(),
    tags: text("tags").notNull().default("[]"),
    createdAt: timestamp("created_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
    updatedAt: timestamp("updated_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
  },
  (table) => [
    index("session_memory_user_idx").on(table.userId),
    index("session_memory_project_idx").on(table.projectId),
    index("session_memory_type_idx").on(table.userId, table.type),
  ]
);

export const colorSchemes = pgTable(
  "color_scheme",
  {
    id: text("id").primaryKey().$type<ColorSchemeId>(),
    name: text("name").notNull(),
    description: text("description"),
    category: text("category").$type<ColorSchemeCategory>().notNull(),
    colorDefinitions: text("color_definitions").notNull(),
    terminalPalette: text("terminal_palette"),
    isBuiltIn: boolean("is_built_in").notNull().default(true),
    sortOrder: integer("sort_order").notNull().default(0),
    createdAt: timestamp("created_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
  },
  (table) => [
    index("color_scheme_category_idx").on(table.category),
    index("color_scheme_sort_idx").on(table.sortOrder),
  ]
);

export const appearanceSettings = pgTable(
  "appearance_settings",
  {
    id: text("id").primaryKey().$defaultFn(() => crypto.randomUUID()),
    userId: text("user_id").notNull().unique().references(() => users.id, { onDelete: "cascade" }),
    appearanceMode: text("appearance_mode").$type<AppearanceMode>().notNull().default("system"),
    lightColorScheme: text("light_color_scheme").$type<ColorSchemeId>().notNull().default("ocean"),
    darkColorScheme: text("dark_color_scheme").$type<ColorSchemeId>().notNull().default("midnight"),
    terminalOpacity: integer("terminal_opacity").notNull().default(100),
    terminalBlur: integer("terminal_blur").notNull().default(0),
    terminalCursorStyle: text("terminal_cursor_style").$type<"block" | "underline" | "bar">().notNull().default("block"),
    createdAt: timestamp("created_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
    updatedAt: timestamp("updated_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
  },
  (table) => [
    index("appearance_settings_user_idx").on(table.userId),
  ]
);

export const agentProfileJsonConfigs = pgTable(
  "agent_profile_json_config",
  {
    id: text("id").primaryKey().$defaultFn(() => crypto.randomUUID()),
    profileId: text("profile_id").notNull().references(() => agentProfiles.id, { onDelete: "cascade" }),
    userId: text("user_id").notNull().references(() => users.id, { onDelete: "cascade" }),
    agentType: text("agent_type").$type<Exclude<AgentProvider, "all">>().notNull(),
    configJson: text("config_json").notNull().default("{}"),
    isValid: boolean("is_valid").notNull().default(true),
    validationErrors: text("validation_errors"),
    createdAt: timestamp("created_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
    updatedAt: timestamp("updated_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
  },
  (table) => [
    index("agent_profile_json_config_profile_idx").on(table.profileId),
    index("agent_profile_json_config_user_idx").on(table.userId),
    uniqueIndex("agent_profile_json_config_unique_idx").on(table.profileId, table.agentType),
  ]
);

export const profileAppearanceSettings = pgTable(
  "profile_appearance_settings",
  {
    id: text("id").primaryKey().$defaultFn(() => crypto.randomUUID()),
    profileId: text("profile_id").notNull().unique().references(() => agentProfiles.id, { onDelete: "cascade" }),
    userId: text("user_id").notNull().references(() => users.id, { onDelete: "cascade" }),
    appearanceMode: text("appearance_mode").$type<AppearanceMode>().notNull().default("system"),
    lightColorScheme: text("light_color_scheme").$type<ColorSchemeId>().notNull().default("ocean"),
    darkColorScheme: text("dark_color_scheme").$type<ColorSchemeId>().notNull().default("midnight"),
    terminalOpacity: integer("terminal_opacity").notNull().default(100),
    terminalBlur: integer("terminal_blur").notNull().default(0),
    terminalCursorStyle: text("terminal_cursor_style").$type<"block" | "underline" | "bar">().notNull().default("block"),
    createdAt: timestamp("created_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
    updatedAt: timestamp("updated_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
  },
  (table) => [
    index("profile_appearance_profile_idx").on(table.profileId),
    index("profile_appearance_user_idx").on(table.userId),
  ]
);

export const projectTasks = pgTable(
  "project_task",
  {
    id: text("id").primaryKey().$defaultFn(() => crypto.randomUUID()),
    userId: text("user_id").notNull().references(() => users.id, { onDelete: "cascade" }),
    projectId: text("project_id").notNull().references(() => projects.id, { onDelete: "cascade" }),
    sessionId: text("session_id").references(() => terminalSessions.id, { onDelete: "set null" }),
    title: text("title").notNull(),
    description: text("description"),
    status: text("status").$type<TaskStatus>().notNull().default("open"),
    priority: text("priority").$type<TaskPriority>().notNull().default("medium"),
    source: text("source").$type<TaskSource>().notNull().default("manual"),
    labels: text("labels").notNull().default("[]"),
    subtasks: text("subtasks").notNull().default("[]"),
    metadata: text("metadata").notNull().default("{}"),
    instructions: text("instructions"),
    agentTaskKey: text("agent_task_key"),
    owner: text("owner"),
    dueDate: timestamp("due_date", { withTimezone: true, mode: "date" }),
    githubIssueUrl: text("github_issue_url"),
    sortOrder: integer("sort_order").notNull().default(0),
    createdAt: timestamp("created_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
    updatedAt: timestamp("updated_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
  },
  (table) => [
    index("project_task_user_idx").on(table.userId),
    index("project_task_project_idx").on(table.projectId),
    index("project_task_user_project_idx").on(table.userId, table.projectId),
    index("project_task_session_idx").on(table.sessionId),
    index("project_task_agent_key_idx").on(table.sessionId, table.agentTaskKey),
  ]
);

export const taskDependencies = pgTable(
  "task_dependency",
  {
    blockerId: text("blocker_id").notNull().references(() => projectTasks.id, { onDelete: "cascade" }),
    blockedId: text("blocked_id").notNull().references(() => projectTasks.id, { onDelete: "cascade" }),
  },
  (table) => [
    primaryKey({ columns: [table.blockerId, table.blockedId] }),
    index("task_dep_blocker_idx").on(table.blockerId),
    index("task_dep_blocked_idx").on(table.blockedId),
  ]
);

export const notificationEvents = pgTable(
  "notification_event",
  {
    id: text("id").primaryKey().$defaultFn(() => crypto.randomUUID()),
    userId: text("user_id").notNull().references(() => users.id, { onDelete: "cascade" }),
    sessionId: text("session_id").references(() => terminalSessions.id, { onDelete: "set null" }),
    sessionName: text("session_name"),
    type: text("type").$type<NotificationType>().notNull(),
    severity: text("severity").$type<NotificationSeverity>().notNull().default("passive"),
    title: text("title").notNull(),
    body: text("body"),
    coalesceKey: text("coalesce_key"),
    count: integer("count").notNull().default(1),
    meta: jsonb("meta").$type<NotificationMeta | null>(),
    readAt: timestamp("read_at", { withTimezone: true, mode: "date" }),
    createdAt: timestamp("created_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
    updatedAt: timestamp("updated_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
  },
  (table) => [
    index("notification_event_user_created_idx").on(table.userId, table.createdAt),
    index("notification_event_user_read_idx").on(table.userId, table.readAt),
    index("notification_event_coalesce_idx").on(table.userId, table.sessionId, table.coalesceKey, table.readAt),
  ]
);

export const pushTokens = pgTable(
  "push_token",
  {
    id: text("id").primaryKey().$defaultFn(() => crypto.randomUUID()),
    userId: text("user_id").notNull().references(() => users.id, { onDelete: "cascade" }),
    fcmToken: text("fcm_token").notNull(),
    platform: text("platform").$type<"android" | "ios">().notNull(),
    deviceId: text("device_id"),
    createdAt: timestamp("created_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
    updatedAt: timestamp("updated_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
  },
  (table) => [
    index("push_token_user_idx").on(table.userId),
    uniqueIndex("push_token_fcm_token_idx").on(table.fcmToken),
  ]
);

export const notificationPreferences = pgTable(
  "notification_preferences",
  {
    userId: text("user_id").primaryKey().references(() => users.id, { onDelete: "cascade" }),
    pushByType: jsonb("push_by_type").$type<Record<string, boolean>>().notNull().default({}),
    mutedSessionIds: jsonb("muted_session_ids").$type<string[]>().notNull().default([]),
    quietHoursStart: integer("quiet_hours_start"),
    quietHoursEnd: integer("quiet_hours_end"),
    minPushSeverity: text("min_push_severity").$type<NotificationSeverity>().notNull().default("actionable"),
    updatedAt: timestamp("updated_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
  }
);

export const agentPeerMessages = pgTable(
  "agent_peer_message",
  {
    id: text("id").primaryKey().$defaultFn(() => crypto.randomUUID()),
    projectId: text("project_id").notNull(),
    fromSessionId: text("from_session_id").references(() => terminalSessions.id, { onDelete: "set null" }),
    fromSessionName: text("from_session_name").notNull(),
    toSessionId: text("to_session_id").references(() => terminalSessions.id, { onDelete: "set null" }),
    body: text("body").notNull(),
    isUserMessage: boolean("is_user_message").notNull().default(false),
    channelId: text("channel_id").references(() => channels.id, { onDelete: "set null" }),
    parentMessageId: text("parent_message_id"),
    replyCount: integer("reply_count").notNull().default(0),
    createdAt: timestamp("created_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
  },
  (table) => [
    index("peer_message_project_created_idx").on(table.projectId, table.createdAt),
    index("peer_message_to_session_idx").on(table.toSessionId),
    index("peer_message_channel_created_idx").on(table.channelId, table.createdAt),
    index("peer_message_parent_idx").on(table.parentMessageId),
  ]
);

export const channelGroups = pgTable(
  "channel_group",
  {
    id: text("id").primaryKey().$defaultFn(() => crypto.randomUUID()),
    projectId: text("project_id").notNull().references(() => projects.id, { onDelete: "cascade" }),
    name: text("name").notNull(),
    position: integer("position").notNull().default(0),
    createdAt: timestamp("created_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
  },
  (table) => [
    index("channel_group_project_idx").on(table.projectId),
    uniqueIndex("channel_group_project_name_idx").on(table.projectId, table.name),
  ]
);

export const channels = pgTable(
  "channel",
  {
    id: text("id").primaryKey().$defaultFn(() => crypto.randomUUID()),
    projectId: text("project_id").notNull().references(() => projects.id, { onDelete: "cascade" }),
    groupId: text("group_id").notNull().references(() => channelGroups.id, { onDelete: "cascade" }),
    name: text("name").notNull(),
    displayName: text("display_name").notNull(),
    type: text("type").$type<ChannelType>().notNull().default("public"),
    topic: text("topic"),
    isDefault: boolean("is_default").notNull().default(false),
    createdBySessionId: text("created_by_session_id"),
    lastMessageAt: timestamp("last_message_at", { withTimezone: true, mode: "date" }),
    messageCount: integer("message_count").notNull().default(0),
    archivedAt: timestamp("archived_at", { withTimezone: true, mode: "date" }),
    createdAt: timestamp("created_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
  },
  (table) => [
    index("channel_project_idx").on(table.projectId),
    index("channel_group_idx").on(table.groupId),
    uniqueIndex("channel_project_name_idx").on(table.projectId, table.name),
  ]
);

export const channelReadState = pgTable(
  "channel_read_state",
  {
    id: text("id").primaryKey().$defaultFn(() => crypto.randomUUID()),
    channelId: text("channel_id").notNull().references(() => channels.id, { onDelete: "cascade" }),
    userId: text("user_id").notNull().references(() => users.id, { onDelete: "cascade" }),
    lastReadMessageId: text("last_read_message_id"),
    lastReadAt: timestamp("last_read_at", { withTimezone: true, mode: "date" }),
  },
  (table) => [
    uniqueIndex("channel_read_state_unique_idx").on(table.channelId, table.userId),
    index("channel_read_state_user_idx").on(table.userId),
  ]
);

export const systemUpdateCache = pgTable(
  "system_update_cache",
  {
    id: text("id").primaryKey().default("singleton"),
    lastChecked: timestamp("last_checked", { withTimezone: true, mode: "date" }),
    cachedReleaseJson: text("cached_release_json"),
    deploymentStateJson: text("deployment_state_json"),
    updatedAt: timestamp("updated_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
  }
);

export const litellmConfig = pgTable(
  "litellm_config",
  {
    id: text("id").primaryKey().$defaultFn(() => crypto.randomUUID()),
    userId: text("user_id").notNull().unique().references(() => users.id, { onDelete: "cascade" }),
    enabled: boolean("enabled").notNull().default(false),
    autoStart: boolean("auto_start").notNull().default(true),
    port: integer("port").notNull().default(4000),
    masterKey: text("master_key"),
    createdAt: timestamp("created_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
    updatedAt: timestamp("updated_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
  }
);

export const litellmModels = pgTable(
  "litellm_model",
  {
    id: text("id").primaryKey().$defaultFn(() => crypto.randomUUID()),
    userId: text("user_id").notNull().references(() => users.id, { onDelete: "cascade" }),
    modelName: text("model_name").notNull(),
    provider: text("provider").notNull(),
    litellmModel: text("litellm_model").notNull(),
    apiBase: text("api_base"),
    encryptedApiKey: text("encrypted_api_key"),
    keyPrefix: text("key_prefix"),
    extraHeaders: text("extra_headers"),
    priority: integer("priority").notNull().default(0),
    paused: boolean("paused").notNull().default(false),
    isDefault: boolean("is_default").notNull().default(false),
    createdAt: timestamp("created_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
    updatedAt: timestamp("updated_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
  },
  (table) => [
    index("litellm_model_user_idx").on(table.userId),
  ]
);

export const projectGroups = pgTable(
  "project_group",
  {
    id: text("id").primaryKey(),
    userId: text("user_id").notNull().references(() => users.id, { onDelete: "cascade" }),
    parentGroupId: text("parent_group_id").references((): AnyPgColumn => projectGroups.id, { onDelete: "set null" }),
    name: text("name").notNull(),
    collapsed: boolean("collapsed").notNull().default(false),
    sortOrder: integer("sort_order").notNull().default(0),
    createdAt: timestamp("created_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
    updatedAt: timestamp("updated_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
  },
  (table) => [
    index("project_group_user_idx").on(table.userId),
    index("project_group_parent_idx").on(table.parentGroupId),
  ]
);

export const projects = pgTable(
  "project",
  {
    id: text("id").primaryKey(),
    userId: text("user_id").notNull().references(() => users.id, { onDelete: "cascade" }),
    groupId: text("group_id").references(() => projectGroups.id, { onDelete: "set null" }),
    name: text("name").notNull(),
    collapsed: boolean("collapsed").notNull().default(false),
    sortOrder: integer("sort_order").notNull().default(0),
    isAutoCreated: boolean("is_auto_created").notNull().default(false),
    createdAt: timestamp("created_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
    updatedAt: timestamp("updated_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
  },
  (table) => [
    index("project_user_idx").on(table.userId),
    index("project_group_idx").on(table.groupId),
  ]
);

export const nodePreferences = pgTable(
  "node_preferences",
  {
    id: text("id").primaryKey(),
    ownerId: text("owner_id").notNull(),
    ownerType: text("owner_type", { enum: ["group","project"] }).notNull(),
    userId: text("user_id").notNull().references(() => users.id, { onDelete: "cascade" }),
    defaultWorkingDirectory: text("default_working_directory"),
    defaultShell: text("default_shell"),
    startupCommand: text("startup_command"),
    theme: text("theme"),
    fontSize: integer("font_size"),
    fontFamily: text("font_family"),
    githubRepoId: text("github_repo_id"),
    localRepoPath: text("local_repo_path"),
    defaultAgentProvider: text("default_agent_provider"),
    agentProviderSettings: jsonb("agent_provider_settings"),
    environmentVars: jsonb("environment_vars"),
    pinnedFiles: jsonb("pinned_files"),
    gitIdentityName: text("git_identity_name"),
    gitIdentityEmail: text("git_identity_email"),
    isSensitive: boolean("is_sensitive").notNull().default(false),
    createdAt: timestamp("created_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
    updatedAt: timestamp("updated_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
  },
  (table) => [
    index("node_pref_owner_idx").on(table.ownerId, table.ownerType),
    uniqueIndex("node_pref_owner_user_idx").on(table.ownerId, table.ownerType, table.userId),
  ]
);

export const projectSecretsConfig = pgTable(
  "project_secrets_config",
  {
    id: text("id").primaryKey(),
    userId: text("user_id").notNull().references(() => users.id, { onDelete: "cascade" }),
    projectId: text("project_id").notNull().references(() => projects.id, { onDelete: "cascade" }),
    provider: text("provider").notNull(),
    providerConfig: jsonb("provider_config").notNull(),
    enabled: boolean("enabled").notNull().default(true),
    lastFetchedAt: timestamp("last_fetched_at", { withTimezone: true, mode: "date" }),
    createdAt: timestamp("created_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
    updatedAt: timestamp("updated_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
  },
  (table) => [
    uniqueIndex("project_secrets_project_user_idx").on(table.projectId, table.userId),
  ]
);

export const projectGitHubAccountLinks = pgTable(
  "project_github_account_link",
  {
    projectId: text("project_id").primaryKey().references(() => projects.id, { onDelete: "cascade" }),
    providerAccountId: text("provider_account_id").notNull(),
    createdAt: timestamp("created_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
  },
  (table) => [
    index("project_gh_link_account_idx").on(table.providerAccountId),
  ]
);

export const projectProfileLinks = pgTable(
  "project_profile_link",
  {
    projectId: text("project_id").primaryKey().references(() => projects.id, { onDelete: "cascade" }),
    profileId: text("profile_id").notNull().references(() => agentProfiles.id, { onDelete: "cascade" }),
    createdAt: timestamp("created_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
  },
  (table) => [
    index("project_profile_link_profile_idx").on(table.profileId),
  ]
);

export const projectRepositories = pgTable(
  "project_repository",
  {
    id: text("id").primaryKey().$defaultFn(() => crypto.randomUUID()),
    projectId: text("project_id").notNull().references(() => projects.id, { onDelete: "cascade" }),
    repositoryId: text("repository_id").notNull().references(() => githubRepositories.id, { onDelete: "cascade" }),
    userId: text("user_id").notNull().references(() => users.id, { onDelete: "cascade" }),
    createdAt: timestamp("created_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
  },
  (table) => [
    uniqueIndex("project_repo_project_user_idx").on(table.projectId, table.userId),
    index("project_repo_user_idx").on(table.userId),
  ]
);

export const sshConnections = pgTable(
  "ssh_connection",
  {
    id: text("id").primaryKey().$defaultFn(() => crypto.randomUUID()),
    userId: text("user_id").notNull().references(() => users.id, { onDelete: "cascade" }),
    projectId: text("project_id").references(() => projects.id, { onDelete: "set null" }),
    name: text("name").notNull(),
    host: text("host").notNull(),
    port: integer("port").notNull().default(22),
    username: text("username").notNull(),
    authType: text("auth_type", { enum: ["key","agent","password","system"] }).notNull(),
    hasPassphrase: boolean("has_passphrase").notNull().default(false),
    passwordEnc: text("password_enc"),
    knownHostsPolicy: text("known_hosts_policy", { enum: ["strict","accept-new","no"] }).notNull().default("accept-new"),
    extraOptions: jsonb("extra_options").$type<string[]>(),
    createdAt: timestamp("created_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
    updatedAt: timestamp("updated_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
    lastUsedAt: timestamp("last_used_at", { withTimezone: true, mode: "date" }),
  },
  (table) => [
    index("ssh_connection_user_project_idx").on(table.userId, table.projectId),
  ]
);

export const triggerConfigs = pgTable(
  "trigger_config",
  {
    id: text("id").primaryKey().$defaultFn(() => crypto.randomUUID()),
    userId: text("user_id").notNull().references(() => users.id, { onDelete: "cascade" }),
    projectId: text("project_id").notNull().references(() => projects.id, { onDelete: "cascade" }),
    githubRepoId: text("github_repo_id").references(() => githubRepositories.id, { onDelete: "cascade" }),
    name: text("name").notNull(),
    kind: text("kind").$type<TriggerKind>().notNull(),
    filter: text("filter").notNull().default("{}"),
    agentProvider: text("agent_provider").notNull().default("claude"),
    agentFlags: text("agent_flags").notNull().default("[]"),
    promptTemplate: text("prompt_template").notNull(),
    worktreeType: text("worktree_type"),
    enabled: boolean("enabled").notNull().default(true),
    createdAt: timestamp("created_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
    updatedAt: timestamp("updated_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
  },
  (table) => [
    index("trigger_config_user_idx").on(table.userId),
    index("trigger_config_repo_idx").on(table.githubRepoId),
  ]
);

export const agentSchedules = pgTable(
  "agent_schedule",
  {
    id: text("id").primaryKey().$defaultFn(() => crypto.randomUUID()),
    userId: text("user_id").notNull().references(() => users.id, { onDelete: "cascade" }),
    projectId: text("project_id").notNull().references(() => projects.id, { onDelete: "cascade" }),
    name: text("name").notNull(),
    agentProvider: text("agent_provider").notNull().default("claude"),
    agentFlags: text("agent_flags").notNull().default("[]"),
    prompt: text("prompt").notNull(),
    worktreeType: text("worktree_type"),
    baseBranch: text("base_branch"),
    scheduleType: text("schedule_type").$type<ScheduleType>().notNull().default("recurring"),
    cronExpression: text("cron_expression"),
    scheduledAt: timestamp("scheduled_at", { withTimezone: true, mode: "date" }),
    timezone: text("timezone").notNull().default("America/Los_Angeles"),
    enabled: boolean("enabled").notNull().default(true),
    status: text("status").$type<ScheduleStatus>().notNull().default("active"),
    maxRetries: integer("max_retries").notNull().default(0),
    nextRunAt: timestamp("next_run_at", { withTimezone: true, mode: "date" }),
    lastRunAt: timestamp("last_run_at", { withTimezone: true, mode: "date" }),
    consecutiveFailures: integer("consecutive_failures").notNull().default(0),
    createdAt: timestamp("created_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
    updatedAt: timestamp("updated_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
  },
  (table) => [
    index("agent_schedule_user_idx").on(table.userId),
    index("agent_schedule_next_run_idx").on(table.enabled, table.nextRunAt),
  ]
);

export const agentRuns = pgTable(
  "agent_run",
  {
    id: text("id").primaryKey().$defaultFn(() => crypto.randomUUID()),
    userId: text("user_id").notNull().references(() => users.id, { onDelete: "cascade" }),
    projectId: text("project_id").notNull().references(() => projects.id, { onDelete: "cascade" }),
    scheduleId: text("schedule_id").references(() => agentSchedules.id, { onDelete: "set null" }),
    triggerConfigId: text("trigger_config_id").references(() => triggerConfigs.id, { onDelete: "set null" }),
    source: text("source").$type<AgentRunSource>().notNull(),
    agentProvider: text("agent_provider").notNull(),
    agentFlags: text("agent_flags").notNull().default("[]"),
    prompt: text("prompt").notNull(),
    sessionId: text("session_id").references(() => terminalSessions.id, { onDelete: "set null" }),
    headSha: text("head_sha"),
    status: text("status").$type<AgentRunStatus>().notNull().default("pending"),
    errorMessage: text("error_message"),
    startedAt: timestamp("started_at", { withTimezone: true, mode: "date" }),
    completedAt: timestamp("completed_at", { withTimezone: true, mode: "date" }),
    createdAt: timestamp("created_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
  },
  (table) => [
    index("agent_run_user_idx").on(table.userId),
    index("agent_run_schedule_idx").on(table.scheduleId),
    index("agent_run_trigger_idx").on(table.triggerConfigId),
    index("agent_run_status_idx").on(table.status),
    uniqueIndex("agent_run_trigger_head_idx").on(table.triggerConfigId, table.headSha),
  ]
);

export const triggerEvents = pgTable(
  "trigger_event",
  {
    id: text("id").primaryKey().$defaultFn(() => crypto.randomUUID()),
    triggerConfigId: text("trigger_config_id").notNull().references(() => triggerConfigs.id, { onDelete: "cascade" }),
    eventKind: text("event_kind").notNull(),
    action: text("action"),
    headSha: text("head_sha"),
    matched: boolean("matched").notNull().default(false),
    runId: text("run_id").references(() => agentRuns.id, { onDelete: "set null" }),
    createdAt: timestamp("created_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
  },
  (table) => [
    index("trigger_event_config_idx").on(table.triggerConfigId),
  ]
);

export const webhookDeliveries = pgTable(
  "webhook_delivery",
  {
    deliveryId: text("delivery_id").primaryKey(),
    eventKind: text("event_kind").notNull(),
    receivedAt: timestamp("received_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
  },
  (table) => [
    index("webhook_delivery_received_idx").on(table.receivedAt),
  ]
);

export const crownRuns = pgTable(
  "crown_run",
  {
    id: text("id").primaryKey().$defaultFn(() => crypto.randomUUID()),
    userId: text("user_id").notNull().references(() => users.id, { onDelete: "cascade" }),
    projectId: text("project_id").notNull().references(() => projects.id, { onDelete: "cascade" }),
    prompt: text("prompt").notNull(),
    agentProvider: text("agent_provider").notNull().default("claude"),
    candidateCount: integer("candidate_count").notNull(),
    judgeModel: text("judge_model"),
    baseBranch: text("base_branch"),
    status: text("status").$type<CrownRunStatus>().notNull().default("running"),
    winnerCandidateId: text("winner_candidate_id"),
    crownReason: text("crown_reason"),
    prUrl: text("pr_url"),
    errorMessage: text("error_message"),
    createdAt: timestamp("created_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
  },
  (table) => [
    index("crown_run_user_idx").on(table.userId),
  ]
);

export const crownCandidates = pgTable(
  "crown_candidate",
  {
    id: text("id").primaryKey().$defaultFn(() => crypto.randomUUID()),
    crownRunId: text("crown_run_id").notNull().references(() => crownRuns.id, { onDelete: "cascade" }),
    runId: text("run_id").references(() => agentRuns.id, { onDelete: "set null" }),
    branch: text("branch").notNull(),
    worktreePath: text("worktree_path"),
    diff: text("diff"),
    diffStats: text("diff_stats"),
    createdAt: timestamp("created_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
  },
  (table) => [
    index("crown_candidate_run_idx").on(table.crownRunId),
  ]
);

export const messageDelivery = pgTable(
  "message_delivery",
  {
    id: text("id").primaryKey().$defaultFn(() => crypto.randomUUID()),
    messageId: text("message_id").notNull().references(() => agentPeerMessages.id, { onDelete: "cascade" }),
    toSessionId: text("to_session_id").notNull().references(() => terminalSessions.id, { onDelete: "cascade" }),
    projectId: text("project_id").notNull(),
    state: text("state").$type<"pending" | "delivered" | "acked">().notNull().default("pending"),
    channelType: text("channel_kind").$type<"mcp_push" | "poll" | null>(),
    deliveredAt: timestamp("delivered_at", { withTimezone: true, mode: "date" }),
    ackedAt: timestamp("acked_at", { withTimezone: true, mode: "date" }),
    createdAt: timestamp("created_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
  },
  (table) => [
    uniqueIndex("message_delivery_msg_session_idx").on(table.messageId, table.toSessionId),
    index("message_delivery_session_state_idx").on(table.toSessionId, table.state, table.createdAt),
  ]
);

export const channelSubscription = pgTable(
  "channel_subscription",
  {
    id: text("id").primaryKey().$defaultFn(() => crypto.randomUUID()),
    channelId: text("channel_id").notNull().references(() => channels.id, { onDelete: "cascade" }),
    sessionId: text("session_id").notNull().references(() => terminalSessions.id, { onDelete: "cascade" }),
    mode: text("mode").$type<"auto_deliver" | "direct_only">().notNull().default("auto_deliver"),
    createdAt: timestamp("created_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
  },
  (table) => [
    uniqueIndex("channel_subscription_unique_idx").on(table.channelId, table.sessionId),
  ]
);

export const agentWorkContext = pgTable(
  "agent_work_context",
  {
    sessionId: text("session_id").primaryKey().references(() => terminalSessions.id, { onDelete: "cascade" }),
    projectId: text("project_id").notNull(),
    branch: text("branch"),
    worktreePath: text("worktree_path"),
    activityStatus: text("activity_status"),
    claimedIssueId: text("claimed_issue_id"),
    claimedIssueTitle: text("claimed_issue_title"),
    joinConfidence: text("join_confidence").$type<"branch" | "project" | "none">(),
    updatedAt: timestamp("updated_at", { withTimezone: true, mode: "date" }).notNull().$defaultFn(() => new Date()),
  },
  (table) => [
    index("agent_work_context_project_idx").on(table.projectId),
  ]
);
