{
  "name": "@remote-dev/supervisor-router-e2e",
  "version": "0.1.0",
  "description": "Containerized E2E smoke harness for the Supervisor single-front-door router (spec §11 M8 / bd remote-dev-jvcx.11): boots the supervisor + router + 2 slug-aware instances via docker-compose and asserts login at root and /<slug>/ plus a live WS terminal THROUGH THE ROUTER.",
  "private": true,
  "type": "module",
  "scripts": {
    "smoke": "bun smoke.ts",
    "seed": "bun seed-instances.ts",
    "e2e": "bash run.sh",
    "typecheck": "tsc --noEmit",
    "lint": "eslint ."
  },
  "devDependencies": {
    "@eslint/js": "^9",
    "@libsql/client": "^0.15.15",
    "bun-types": "^1.3.14",
    "eslint": "^9",
    "globals": "^16",
    "typescript": "^5",
    "typescript-eslint": "^8.57.1"
  }
}
